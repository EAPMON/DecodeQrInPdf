{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "bash install.sh && hypercorn main:app --bind \"[::]:$PORT\""
  }
}